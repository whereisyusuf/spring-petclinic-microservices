spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/clarenceb/spring-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
